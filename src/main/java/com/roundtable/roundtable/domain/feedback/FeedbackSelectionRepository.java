package com.roundtable.roundtable.domain.feedback;

import org.springframework.data.jpa.repository.JpaRepository;

public interface FeedbackSelectionRepository extends JpaRepository<FeedbackSelection, Long> {
}
